# SUB/ProS — what each W5 figure shows

Generated by `gp_tuning_width.write_region_reports` on 2026-09-09 08:23, from the executed notebook. Figures live in `/ceph/behrens/adam_harris/Taskspace_abstraction_lEC/data/figures/gp_tuning_width`; the method is `code/GP_TUNING_WIDTH.md`, the plan `docs/handoff/W5_gp_tuning_width.md`.

## This region in one table

| | |
|---|---|
| unit-recordings shown | 322 |
| mice / recdays | 4 / 20 |
| width defined (peak reproduced in the held-out half) | 94.7% |
| median split-half r | 0.933 |
| median peak phase | 0.406 |
| median half-max width (fraction of leg) | 0.511 |
| median circular-SD width | 0.228 |
| multimodal / ramp | 14.0% / 5.9% |
| peak moves > 30 bins across sessions | 46.6% |
| median firing rate (Hz) | 6.62 |

## The two correlations for this region

| statistic | mean over mice | 95 % CI (mouse bootstrap) | mice | per mouse |
|---|---|---|---|---|
| ρ(peak, width) — anchor asymmetry | +0.513 | [+0.406, +0.636] | 4 | {'ah10': 0.379, 'ly05': 0.703, 'ly06': 0.536, 'ly07': 0.433} |
| ρ(\|peak − 0.5\|, width) — the V | -0.534 | [-0.562, -0.507] | 4 | {'ah10': -0.497, 'ly05': -0.572, 'ly06': -0.535, 'ly07': -0.532} |

For comparison, the same statistics on the planted populations that went through this exact code:

| planted population | ρ(peak, width) | ρ(\|peak − 0.5\|, width) | median width |
|---|---|---|---|
| time cells (both anchors) | +0.021 | -0.483 | 0.367 |
| retrospective only | +0.445 | -0.457 | 0.367 |
| prospective only | -0.406 | -0.508 | 0.356 |
| phase cells | +0.003 | +0.011 | 0.250 |
| noise cells | -0.024 | +0.047 | 0.178 |

## Where this region's cells peak, and how wide they are there

| goal-progress bin | cells peaking | share | median width | IQR |
|---|---|---|---|---|
| 0.0 (0.05) | 29 | 9.5% | 0.222 | [0.167, 0.289] |
| 1.0 (0.15) | 76 | 24.9% | 0.367 | [0.308, 0.447] |
| 2.0 (0.25) | 37 | 12.1% | 0.422 | [0.344, 0.522] |
| 3.0 (0.35) | 13 | 4.3% | 0.811 | [0.533, 0.833] |
| 4.0 (0.45) | 33 | 10.8% | 0.778 | [0.533, 0.833] |
| 5.0 (0.55) | 17 | 5.6% | 0.789 | [0.711, 0.811] |
| 6.0 (0.65) | 29 | 9.5% | 0.678 | [0.589, 0.800] |
| 7.0 (0.75) | 10 | 3.3% | 0.694 | [0.647, 0.792] |
| 8.0 (0.85) | 24 | 7.9% | 0.650 | [0.489, 0.739] |
| 9.0 (0.95) | 37 | 12.1% | 0.600 | [0.389, 0.644] |

Spearman across the 10 bins between the count and the median width: **-0.665** (p = 0.036). n here is 10 bins, not 305 cells.

## The figures, one by one

### Sorted beta heatmap, goal progress

`lec_gp_beta_heatmap_{tag}[_all][_regions].pdf`

**Shows.** One row per neuron, ten columns = the ten goal-progress bins of the GLM design, bin 0 at the left. Colour is the fitted reference-coded beta divided by that neuron's own max|beta|, on a diverging scale centred at 0 (blue below the consumption bin, red above it). Rows are sorted by the bin of highest beta, ties broken by the circular centre of mass of the positive part.

**Which cells.** `_all` = every neuron; otherwise only neurons whose `goal_progress` beats its Freedman-Lane null at p < 0.05 in that arm. `_regions` stacks one block per region with a colour strip and block counts.

**Unit of inference.** No aggregation: one row is one unit-recording. Recdays are pooled, so a mouse with more units contributes more rows.

**What it does not show.** NOT smoothed and NOT evidence of tuning. Sorting by peak produces a clean diagonal for any population including pure noise (synthetic control 7); only the structure AROUND the diagonal (how wide the bright band is, whether the row has a second lobe) carries information. Bin 0 is exactly 0 for every neuron by construction, so the colour there is not data.

### Sorted beta heatmap, time from reward

`lec_tfr_beta_heatmap_{tag}[_all][_regions].pdf`

**Shows.** As above for the `time_from_reward` block; columns are seconds (lower bin edge), median over recdays under decile coding.

**Which cells.** tfr-significant neurons of the gp+tfr arms (four of the six).

**Unit of inference.** As above.

**What it does not show.** As above. Under uniform coding the late bins hold under 1 % of rows (GLM_V3.md 9.1), so a late peak rests on very few samples.

### Full-resolution curve heatmap, sorted on itself

`lec_curve_heatmap_sigma{s}.pdf`  — pooled only, this region is not separated in it

**Shows.** One row per gp-significant neuron, 90 columns = phase bins of the leg. Colour is the mean firing rate over all kept legs, z-scored per neuron. Rows sorted by that same curve's peak. `sigma0` is the raw bin means with no smoothing at all; `sigma3` is the same data after 3-bin circular smoothing.

**Which cells.** gp-significant in the uniform/30 arm.

**Unit of inference.** One row per unit-recording, recdays pooled.

**What it does not show.** The diagonal is CIRCULAR here: the sort is defined on the matrix being shown, so it is guaranteed even for noise. Use the cross-validated version for evidence.

### Cross-validated curve heatmap

`lec_curve_heatmap_xval_sigma{s}[_regions].pdf`

**Shows.** Rows sorted by the ODD legs' peak, colours from the EVEN legs (z-scored per neuron). 90 phase bins.

**Which cells.** gp-significant in the uniform/30 arm; the `_regions` version blocks by region.

**Unit of inference.** One row per unit-recording.

**What it does not show.** Here a diagonal IS evidence -- the sort knows nothing about the legs being displayed -- so the honest reading is how much sharper the self-sorted version is than this one.

### Width-estimator calibration (simulated)

`lec_width_calibration.pdf`  — pooled only, this region is not separated in it

**Shows.** Left: recovered half-max width against planted peak phase, one line per planted field width, with the planted value as a dotted line. Right: recovered peak phase against planted peak phase.

**Which cells.** Planted Poisson phase cells on this dataset's real legs, through the same code.

**Unit of inference.** Pooled over recdays and seeds; band is the interquartile range.

**What it does not show.** Nothing about the data. It is the licence to read the width axis at all: a flat line here means the estimator has no peak-dependent bias, i.e. no edge artefact.

### The time-cell null

`lec_time_cell_null.pdf`  — pooled only, this region is not separated in it

**Shows.** Left: recovered width against recovered peak for planted fixed-latency (retrospective) and fixed-lead (prospective) cells, with the planted phase cells as a flat reference. Right: recovered peak phase against the planted latency in seconds.

**Which cells.** Planted cells only.

**Unit of inference.** Median over recdays and seeds, IQR band.

**What it does not show.** This is the null, not a result. The V it traces is what time coding alone produces once time is divided by a variable leg duration.

### Peak phase against tuning width

`lec_peak_vs_width[_nooverlay][_by_region][_gponly].pdf`

**Shows.** One point per neuron: x = peak phase from the odd legs, y = half-max width from the even legs. Colour is the gp-vs-tfr split of the selecting arm (red leans phase, blue leans absolute time). Marginal histograms on the pooled version. The overlay is the simulated time-cell V and the planted phase-cell line; `_nooverlay` is the same data with no simulation drawn.

**Which cells.** gp-significant in the uniform/30 arm and with a defined width; `_gponly` repeats it for the gp-only/30 significant set.

**Unit of inference.** One point per unit-recording -- this panel is descriptive. The inference is the per-mouse rho figures.

**What it does not show.** A relation here is NOT evidence of anything until it is compared with the overlaid V, which time coding produces on its own.

### How many cells peak in a bin against how wide they are

`lec_peak_count_vs_width.pdf`

**Shows.** Left: bars = number of gp-significant cells whose peak falls in each of ten goal-progress bins; line (right axis) = the median half-max width of exactly those cells, one line per region when regions are drawn. Right: the same two quantities against each other, one point per bin, labelled by phase, with the Spearman across bins and the simulated populations as crosses.

**Which cells.** gp-significant in the uniform/30 arm with a defined width.

**Unit of inference.** Bins pool every unit-recording; the correlation is across the ten bins, not across cells, so its n is 10 and it carries no per-mouse inference.

**What it does not show.** Count and width are two summaries of the same cells: this correlation is the V multiplied by the peak distribution, so its sign follows from where peaks pile up (at the leg edges in this data, hence negative) and it is not independent evidence for anything.

### Peak distribution, one panel per region

`lec_peak_distribution_by_region.pdf`

**Shows.** One panel per region. Bars are the fraction of THAT region's cells peaking in each of ten goal-progress bins — a fraction, not a count, so regions with very different n share an axis. The black line on the right axis is that region's median half-max width per bin. A final panel overlays every region's fraction curve.

**Which cells.** gp-significant in the uniform/30 arm with a defined width.

**Unit of inference.** Bars pool every unit-recording of the region; the panel title gives n cells and n mice. No per-mouse inference is drawn here — it is a description.

**What it does not show.** The companion figure `{p}_peak_count_vs_width` pools the bars over all regions and is the one to read for the count-width relation; this one exists to compare the peak DISTRIBUTIONS, which that figure hides. A difference between the single-mouse panels (ENTl-sup, and ENTm on two) and the rest is a difference between animals as much as between regions.

### The two correlations, per region, mice as the unit

`lec_rho_peak_phase / lec_rho_abs_peak_off_centre.pdf`

**Shows.** Per region: each point is one mouse (its recdays' Spearman rho averaged), the wide dash the mean over mice, the vertical bar a mouse-resampling bootstrap CI, the number below the count of mice. Dashed horizontal lines are the same statistic on the simulated time-cell and phase-cell populations.

**Which cells.** gp-significant in the uniform/30 arm; a (recday, region) cell needs at least 8 neurons to contribute.

**Unit of inference.** THIS is the inference panel: neuron -> recday rho -> mouse mean -> mean over mice.

**What it does not show.** A region with one mouse is descriptive and labelled so. rho(peak, width) is the anchor asymmetry; rho(|peak - 0.5|, width) is the V.

### Cross-session peak stability

`lec_peak_stability.pdf`

**Shows.** Left: distribution of the maximum pairwise circular distance between a cell's per-session peaks, in bins of 90, with the 30-bin flag line. Right: that statistic against the circular SD of the same peaks, showing the SD saturating.

**Which cells.** gp-significant cells with peaks in at least two sessions.

**Unit of inference.** One point per unit-recording; the per-region fractions are tabulated beside it.

**What it does not show.** A large value is not necessarily remapping: it also arises when a cell is weakly tuned and its peak is noise. Read it with the split-half r.

### Beta route against curve route

`lec_beta_vs_curve.pdf`  — pooled only, this region is not separated in it

**Shows.** Three scatters: beta peak against curve peak, beta width against curve width, and beta width against beta peak. One point per neuron.

**Which cells.** gp-significant in the uniform/30 arm with both routes defined.

**Unit of inference.** Per unit-recording.

**What it does not show.** Disagreement is a result, not an error: the betas are what goal progress explains GIVEN place, speed, acceleration and time from reward, while the curves are the raw rate.
